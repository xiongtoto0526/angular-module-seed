'use strict';

// @ngInject
export default function($rootScope, $timeout, $state) {
  console.log('enter sp angular run script...')
  // UserService.queryRoles().then(function(result) {
  //   var roleNames = _.pluck(result, 'roleName');
  //   if (roleNames.indexOf('ADMIN') > -1 || roleNames.indexOf('PROJ_M') > -1) {
  //     $state.go('details');
  //   } else if (roleNames.indexOf('PLAT_M') > -1) {
  //     $state.go('share-edit');
  //   } else {
  //     $state.go('no-privilege');
  //   }
  // });
}
